# Rust build artifacts
/target/
**/*.rs.bk
*.pdb

# Cargo.lock for libraries (keep for binaries)
# Cargo.lock

# Database files - NEVER commit these
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite3
data/*.db
data/*.db-*

# Backups
/backups/
data/backups/

# Environment files
.env
.env.local

# IDE / Editor
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# OS files
Thumbs.db
Desktop.ini

# Logs
*.log
logs/

# Temporary files
tmp/
temp/
*.tmp

# Reports and generated files
reports/
workspace/
tasks/

# Test coverage
*.profraw
*.profdata
coverage/

# Documentation build artifacts
book/
target/doc/

# Cache directories
.cache/

# .rustassistant/ is tracked — it stores cross-run cache state for the
# LLM Audit workflow (llm-audit.yml in nuniesmith/actions).  The workflow
# reads and updates .rustassistant/cache.json, batches/, etc. on every run
# so the repo is self-describing.  Do NOT add .rustassistant/ back here.
